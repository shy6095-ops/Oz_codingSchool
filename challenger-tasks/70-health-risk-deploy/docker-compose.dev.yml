# ── 개발 오버레이 ──
# api 포트를 직접 열고, /docs 를 노출하고, 요청 제한을 느슨하게 둔다.
services:
  api:
    environment:
      APP_ENV: dev
      CORS_ORIGINS: "*"
      RATE_LIMIT_PER_MIN: "1000"
    ports:
      - "8000:8000"
    restart: unless-stopped

  worker:
    restart: unless-stopped
