# FastAPI + Redis 이벤트 기반 아키텍처(EDA) 설계 가이드

## 1. 이벤트 기반 아키텍처(EDA)란?

서비스 간에 **직접 호출**하는 대신, "이벤트"라는 메시지를 통해 간접적으로 소통하는 구조입니다.

```
[일반적인 방식]           [이벤트 기반 방식]
A 서비스 → B 서비스 직접호출     A 서비스 → 이벤트 발행 → 브로커 → B, C, D 서비스가 구독
```

**핵심 이점**
- **느슨한 결합(Loose Coupling)**: A는 B가 존재하는지도 몰라도 됨
- **확장성**: 구독자를 나중에 자유롭게 추가 가능
- **비동기 처리**: 무거운 작업(이메일 발송, 이미지 처리 등)을 즉시 응답 후 백그라운드에서 처리

---

## 2. 구성 요소 3가지

| 요소 | 역할 | 이 아키텍처에서 |
|---|---|---|
| **Producer(발행자)** | 이벤트를 생성해 브로커로 보냄 | FastAPI 엔드포인트 |
| **Broker(중개자)** | 이벤트를 저장/전달 | Redis |
| **Consumer(소비자)** | 이벤트를 받아 실제 작업 수행 | 별도 워커 프로세스 |

---

## 3. Redis, 어떤 기능을 쓸까?

Redis는 EDA에서 3가지 방식으로 쓸 수 있고, 특성이 크게 다릅니다.

| 방식 | 특징 | 메시지 유실 시 | 적합한 경우 |
|---|---|---|---|
| **Pub/Sub** | 실시간 방송, 매우 단순 | 구독자가 없으면 **유실됨** (저장 안 함) | 실시간 알림, 채팅, 순간성 이벤트 |
| **Streams** | 로그처럼 메시지를 저장, Consumer Group 지원 | **유실 안 됨** (ACK 기반 재처리 가능) | 주문 처리, 결제 등 신뢰성 필요한 이벤트 |
| **List (Queue)** | 단순 작업 큐 (RPUSH/BLPOP) | 큐에 쌓여있으면 안전 | 단순 백그라운드 작업 (Celery, RQ와 유사) |

> **실무 팁**: "이벤트가 유실되면 안 되는 비즈니스 로직"이라면 반드시 **Redis Streams**를 사용하세요. Pub/Sub은 구독자가 잠깐이라도 끊기면 그 사이 이벤트는 영구히 사라집니다.

---

## 4. 아키텍처 그림

```
┌─────────────┐    이벤트 발행     ┌──────────────┐    이벤트 소비    ┌─────────────┐
│  FastAPI    │ ───────────────▶ │ Redis Stream  │ ───────────────▶ │  Worker A    │
│  (Producer) │   XADD            │ "order.created"│   XREADGROUP     │ (이메일 발송) │
└─────────────┘                   └──────────────┘                  └─────────────┘
                                          │
                                          │                          ┌─────────────┐
                                          └────────────────────────▶│  Worker B    │
                                                                     │ (재고 차감)   │
                                                                     └─────────────┘
```

FastAPI는 이벤트를 "던지기만" 하고 바로 응답합니다. 실제 무거운 작업은 Worker들이 비동기로 처리합니다.

---

## 5. 실전 코드 예시 (Redis Streams 기준)

### 5-1. 이벤트 발행 (FastAPI Producer)

```python
# main.py
from fastapi import FastAPI
import redis.asyncio as redis
import json
import uuid
from datetime import datetime

app = FastAPI()
redis_client = redis.from_url("redis://localhost:6379")

STREAM_NAME = "order.created"

@app.post("/orders")
async def create_order(product_id: str, quantity: int):
    event = {
        "event_id": str(uuid.uuid4()),
        "event_type": "order.created",
        "product_id": product_id,
        "quantity": str(quantity),
        "timestamp": datetime.utcnow().isoformat(),
    }

    # Redis Stream에 이벤트 추가 (영속 저장됨)
    await redis_client.xadd(STREAM_NAME, event)

    # 무거운 처리를 기다리지 않고 즉시 응답
    return {"status": "accepted", "event_id": event["event_id"]}
```

### 5-2. 이벤트 소비 (별도 Worker 프로세스)

```python
# worker.py
import redis.asyncio as redis
import asyncio

STREAM_NAME = "order.created"
GROUP_NAME = "email-service"
CONSUMER_NAME = "worker-1"

async def main():
    r = redis.from_url("redis://localhost:6379")

    # 컨슈머 그룹 생성 (이미 있으면 무시)
    try:
        await r.xgroup_create(STREAM_NAME, GROUP_NAME, id="0", mkstream=True)
    except redis.ResponseError:
        pass  # 그룹이 이미 존재함

    while True:
        # 새 메시지 읽기 (없으면 최대 5초 대기)
        results = await r.xreadgroup(
            GROUP_NAME, CONSUMER_NAME,
            {STREAM_NAME: ">"}, count=10, block=5000
        )

        for stream, messages in results or []:
            for msg_id, data in messages:
                try:
                    print(f"이메일 발송 처리: {data}")
                    # ... 실제 비즈니스 로직 ...

                    # 처리 완료 -> ACK (재처리 큐에서 제거)
                    await r.xack(STREAM_NAME, GROUP_NAME, msg_id)
                except Exception as e:
                    print(f"처리 실패, 재시도 대상으로 남음: {e}")
                    # ACK 하지 않으면 다음에 재시도 가능

asyncio.run(main())
```

> Worker는 FastAPI 프로세스와 **완전히 분리된 별도 프로세스**로 실행합니다 (`python worker.py`). 필요하면 여러 개 띄워서 수평 확장할 수 있습니다.

---

## 6. 신뢰성을 높이는 추가 패턴

| 패턴 | 설명 |
|---|---|
| **Consumer Group** | 여러 Worker가 하나의 스트림을 나눠서 처리 (부하 분산) |
| **ACK / 재처리** | 처리 실패 시 ACK 안 하면 `XPENDING`으로 재시도 가능 |
| **Dead Letter Queue** | N번 재시도 실패한 이벤트는 별도 스트림(`order.created.dlq`)으로 이동시켜 알람 |
| **Idempotency(멱등성)** | `event_id`를 기준으로 중복 처리를 막음 (같은 이벤트가 2번 소비될 수 있음을 항상 전제) |
| **Outbox 패턴** | DB 트랜잭션과 이벤트 발행을 동시에 보장하고 싶을 때 (DB 저장 → 별도 프로세스가 Redis로 전달) |

---

## 7. 언제 이 구조를 써야 할까?

**적합한 경우**
- 회원가입 → 환영 이메일, 통계 업데이트 등 여러 후속 작업이 뒤따를 때
- API 응답 속도를 빠르게 유지하면서 무거운 작업은 뒤로 미루고 싶을 때
- 여러 팀/서비스가 같은 이벤트를 각자 다른 목적으로 소비해야 할 때

**과할 수 있는 경우**
- 단순 CRUD 서비스, 후속 작업이 거의 없는 경우
- 트래픽이 적어서 굳이 비동기 분리가 필요 없는 경우 → Celery/RQ 없이 `BackgroundTasks`(FastAPI 내장)로 충분할 수 있음

---

## 8. 요약

1. FastAPI는 **이벤트를 던지고 바로 응답**하는 Producer 역할만 합니다.
2. Redis는 **중간 저장소이자 배달부** 역할을 하며, 신뢰성이 필요하면 Pub/Sub이 아닌 **Streams**를 씁니다.
3. 실제 무거운 작업은 **별도 Worker 프로세스**가 비동기로 소비/처리합니다.
4. 실무에서는 여기에 **ACK, 재시도, DLQ, 멱등성** 처리를 더해 신뢰성을 보강합니다.
